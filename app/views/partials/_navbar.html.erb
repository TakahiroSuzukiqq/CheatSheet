<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">CS</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%#= link_to "New Room", new_room_path, class: "button", type: "button" %></li>
        <li><%#= link_to "Rooms", rooms_path, class: "button", type: "button" %></li>
      <% if user_signed_in? %>
        <li>
          <a href="/posts">Go to CheatSheet</a>
        </li>
        <!--<li><img src="/assets/ruby.png" height= 47px width= 47px /></li>-->
        <li>
          <%= link_to 'Post New Method', new_post_path %>
        </li>
      <% end %>
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to('Logout', destroy_user_session_path, method: :delete) %>        
          </li>
          <li>
            <%= link_to(current_user.email, edit_user_registration_path) %>
          </li>
        <% else %>
          <li>
            <%= link_to('Login', new_user_session_path)  %>
          </li>
          <li>
            <%= link_to('Register', new_user_registration_path)  %>
          </li>
        <% end %>
      </ul>
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>